<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>UpGigs – Login</title>
    <link rel="stylesheet" href="/css/upgigs.css" />
    <style>
      body {
        background: var(--background);
        color: var(--foreground);
        font-family: system-ui, sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        padding: 4rem 2rem;
        min-height: 100vh;
        text-align: center;
      }

      h1 {
        font-size: 2.8rem;
        margin-bottom: 1rem;
        color: var(--accent);
        text-shadow: 0 0 8px var(--accent);
      }

      .login-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
        max-width: 700px;
        width: 100%;
        margin-top: 2rem;
      }

      .login-card {
        background: var(--gray-100);
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 1.5rem;
        box-shadow: 0 0 10px var(--gray-50);
        transition: transform 0.2s ease;
      }

      .login-card:hover {
        transform: scale(1.02);
        box-shadow: 0 0 15px var(--blue-300);
      }

      .band-name {
        font-size: 1.2rem;
        font-weight: bold;
        margin-bottom: 1rem;
        color: var(--accent);
      }

      .btn-login {
        background: var(--accent);
        color: var(--background);
        padding: 0.5rem 1.2rem;
        border: none;
        border-radius: 6px;
        font-weight: bold;
        cursor: pointer;
        font-size: 1rem;
      }

      .btn-login:hover {
        box-shadow: 0 0 12px var(--accent);
      }
    </style>
  </head>
  <body>
    <h1>🎸 UpGigs Login</h1>
    <p>Select your band to manage your gigs:</p>

    <div class="login-grid">
      <!-- Jon -->
      <div class="login-card">
        <div class="band-name">Metro Jethros</div>
        <a href="/metro-jethros/admin?token=jon2025token">
          <button class="btn-login">Login</button>
        </a>
      </div>

      <!-- Amy or other band -->
      <div class="login-card">
        <div class="band-name">Mellow Swells</div>
        <a href="/mellow-swells/admin?token=gigs2025tokenX107">
          <button class="btn-login">Login</button>
        </a>
      </div>

      <!-- Another band -->
      <div class="login-card">
        <div class="band-name">Moon Unit</div>
        <a href="/moon-unit/admin?token=gigs2025tokenX107">
          <button class="btn-login">Login</button>
        </a>
      </div>
    </div>
  </body>
</html>
