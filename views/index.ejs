<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Upcoming Gigs | UpGigs</title>
    <link rel="stylesheet" href="/css/upgigs.css" />
  </head>
  <body>
    <div class="container">
      <h1>Upcoming Gigs</h1>

      <% if (gigs && gigs.length > 0) { %> <% gigs.forEach(gig => { %>
      <div class="gig-card">
        <div class="gig-date"><strong><%= gig.date %></strong></div>
        <div class="gig-venue"><%= gig.venue %></div>
        <div class="gig-city"><%= gig.city %></div>
        <% if (gig.time) { %>
        <div class="time">Start Time: <%= gig.time %></div>
        <% } %>
      </div>
      <% }) %> <% } else { %>
      <p>No gigs scheduled.</p>
      <% } %>
    </div>
  </body>
</html>
